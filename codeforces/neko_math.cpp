#include <iostream>
#include <algorithm>
#include <queue>
#include <string.h>
#include <math.h>

#define MAX_N 100005
#define f first
#define s second

using namespace std;

long long A, B;

int main() {
    cin >> A >> B;
    
    int diff = abs(A - B);
    
    for (int i = 2; i * i <= diff; i ++) {
        if (diff % i == 0) {
            if (i * i == diff)
            
            else {
                ans = min(ans, )
            }
        }
    }
    
    return (0);
}