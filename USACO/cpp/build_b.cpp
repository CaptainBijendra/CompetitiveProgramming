#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

typedef pair<int, int> ii;

vector<ii> points;

int N;

bool pos(int d) {
    for (int i = 0; i < N; i ++) {
        for (int j = 0; j < N; j ++) {
            
        }
    }
}

int main() {
    cin >> N;

    int a, b;
    for (int i = 0; i < N; i ++) {
        cin >> a >> b;
        points.push_back(ii(a, b));
    }
}